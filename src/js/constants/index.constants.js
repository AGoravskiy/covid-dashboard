import tableParams from './tables.constants';
import covidAPI from './covidAPI.constants';
import chartParam from './chart.constants';

export default {
  tableParams,
  covidAPI,
  chartParam,
};
